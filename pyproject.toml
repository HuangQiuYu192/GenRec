[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "genrec"
version = "0.1.0"
description = "Modular generative recommendation benchmark"
requires-python = ">=3.9"
dependencies = ["torch>=2.0", "PyYAML>=6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

